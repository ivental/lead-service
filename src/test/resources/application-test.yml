spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/lead_db?currentSchema=public
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: true

app:
  contact-service:
    url: http://localhost:${WIREMOCK_PORT:8083}

